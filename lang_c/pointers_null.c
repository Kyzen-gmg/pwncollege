#include <stdio.h>

int main () {

    int *ptr = NULL;

    printf("The value of ptr is : %x\n", ptr);

    if(ptr)     /* succeeds if ptr is not null */
    if(!ptr)    /* succeeds if ptr is null */


    return 0; //check
}
